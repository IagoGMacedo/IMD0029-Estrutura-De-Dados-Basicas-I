redimensionar(){
    var pra array velho e tamanho velho;
    percorre o novo e define tudo como nullptr
    var pra array novo e tamanho novo;
    
    percorre array velho
    for stdsize i = 0; i<tamanhovelho;i++
        elementoatual = arrayvelho[i]
        se elemento eh valido (dif null e removido){
            inserir no novo array
                atualizar tamanho pro tamanho novo
                indicebase = hash do elemento atual
                //tratando colisao com sondagem linear
                for(std::size delta = 0; delta<tamnovo;delta++){
                    auto indiceAtual = (indiceBase + delta) % tamanhoNovo  
                    if(arrayNovo[indiceAtual] == nullptr{
                        arrayNovo[indiceAtual] =  elementoatual                        
                        break                    
                    }
                }    
    }
}
